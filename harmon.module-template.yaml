# Child modules do not inherit root Kotlin settings; every module applies this template.

settings:
  kotlin:
    version: 2.4.10
    languageVersion: "2.4"
    apiVersion: "2.4"
    progressiveMode: true
    allWarningsAsErrors: true
