# Registers code generation and contributes its output to the enabling module.
tasks:
  generate:
    action: !dev.yoda.harmon.sqldelight.generateDatabase
      sqDir: ${module.rootDir}/sqldelight
      generatedSourceDir: ${taskOutputDir}

generated:
  sources:
    - language: kotlin
      directory: ${tasks.generate.action.generatedSourceDir}
