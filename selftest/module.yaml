# KTC-5573 keeps cinterop out of test compilations, so binding checks use this app. The test suite
# does not link it and SelftestBridgeTest enforces staleness; see docs/native-testing.md.

product:
  type: macos/app
  platforms: [macosArm64]

apply: [../harmon.module-template.yaml]

dependencies:
  - ../bridge-probe
