version: 2
updates:
  # The checked-in wrapper owns the toolchain version; there is no Gradle ecosystem.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      all:
        patterns:
          - "*"
